{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 10000",
    "start": "vite preview --host 0.0.0.0 --port 10000"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
