import React, {useState} from 'react';export default function App(){const [q,setQ]=useState('');return (<div style={{fontFamily:'Arial',padding:24}}><h1>E-earth</h1><form onSubmit={(e)=>{e.preventDefault();location.href='/search?q='+encodeURIComponent(q)}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search anything..." style={{padding:8,width:380}}/><button style={{padding:8,marginLeft:8}}>Search</button></form><p style={{marginTop:20}}>Sample site with search. Click the search button to call backend API.</p></div>)}