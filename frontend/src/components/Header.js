import React, {useState} from 'react';import { useNavigate } from 'react-router-dom';export default function Header(){const [q,setQ]=useState('');const nav = useNavigate();const submit = (e)=>{e.preventDefault(); nav('/search?q='+encodeURIComponent(q));}return (<div className='header'><div className='brand'>E Earth</div><form onSubmit={submit} className='search-bar'><input className='search-input' placeholder='Search anything (e.g. fix motherboard, movie star)' value={q} onChange={e=>setQ(e.target.value)} /><button className='search-btn' type='submit'>Search</button></form></div>)}